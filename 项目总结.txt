1. 缓存问题：一个经过SHA 256计算后的base64编码长度为44B，一次性缓存所有对象散列值是非常缓慢的
2. 断点续传：用户调用接口之后，提供散列值和大小，接口服务POST临时对象接口，在数据节点生成uuid，将uuid和数据节点的地址放入token返回给用户，
   下次用户PUT对象，通过range头部来提供上传的大小，接口服务解析token获取uuid和地址，调用PATCH写入临时对象